> **ARCHIVED / SUPERSEDED (03-09-2026).** Documento histórico conservado
> por auditabilidad. NO trabajar desde aquí: la fuente de verdad vigente
> está indexada en `docs/README.md` (estado real: `ESTADO-PRODUCCION.md`).

# Contexto de la sesión — miércoles 26 de agosto de 2026 (cierre)

Sesión de cierre ordenado sobre `fix/hardening-casamable`. Todo local: sin
deploy, sin tocar Meta/Retell/Shopify reales, sin clientes.

## Qué se integró (PRs de la cola, revisados uno a uno)

| PR | Qué | Nota de la revisión |
|---|---|---|
| #23 | HMAC de Shopify contra AMBOS secretos + salud de firmas en /api/health | Ajuste propio: el aviso "firma con client_secret" sale 1 vez por arranque, no por webhook |
| #19 | /api/health y /live conscientes del proveedor | En la resolución se detectó y corrigió que /live publicaba el **teléfono completo** en un endpoint público |
| #15 | **T1 — BUG bloqueante**: fuera de ventana de 24 h, la confirmación sale como PLANTILLA | Completado con la degradación EN ENVÍO (fila que caduca en cola) + fallos distinguibles |
| #20/#21 | `ordered_at` (fecha real de compra) + MAX_ORDER_AGE contra ella | Esquema **10** (aditivo) |
| #8 | T6: salvaguarda de WhatsApp por grafo TRANSITIVO de imports | |
| #12 | T4: script solo-lectura de pedidos saltados por el backfill | Fixture adaptada al fulfillment por línea de esta rama |
| — | T3 (panel por ordered_at): commit pequeño directo sobre HEAD | El PR #11 tenía un conflicto de 1.880 líneas; el mismo resultado costó 6 líneas aquí |

## Qué se construyó nuevo

- **Llamadas fail-closed**: con TEST_MODE=1, `CALLS_ALLOWLIST` vacía ahora
  BLOQUEA a todos (antes: abría a todos — la trampa nº 1 de la lista).
- **Validador de prompt de Retell** (`npm run calls:validate-prompt`): caza
  los marcadores del incidente v5; contrato sincronizado con payload.ts por
  test.
- **Salud de llamadas** en el overview: racha de fallos, y el saldo dicho
  como "comprobación manual" — nunca un healthy falso.
- **Diagnóstico Dropi** (`npm run dropi:diagnose`): vendor esperado vs real
  (la causa del 23-08) y las 4 causas posibles de `sku=null`, solo lectura.
- **Dropi sin API**: cabeceras ⛔ en el andamiaje + clasificación completa en
  DROPI-API-CONTRACT.md para que nadie "termine" una API que no existe.

## Decisiones

- **No hay botón de "llamada de prueba" en el panel** a propósito: un botón
  que origina llamadas es más riesgo que valor. Método seguro documentado.
- **Política de outbox mixto** (ya de ayer, reconfirmada): proveedor
  resuelto al ENVIAR; el fallback textual viaja en `content`.
- **main reconciliado por merge explícito** al cierre de esta sesión (main
  era ancestro puro, 0 commits exclusivos): `main` vuelve a representar el
  código candidato. **El NAS no se ha tocado** y sigue en la rama.

## Cola de PRs al cierre

Integrados: #8, #12, #15, #19, #20, #21, #23. Recomendados CERRAR como
supersedidos: #3, #4 (contenido en main+producción, base obsoleta), #7
(.env.example ya reorganizado y sincronizado en esta rama), #9, #10, #11
(variantes para main de lo ya integrado), #14, #17 (sus variantes
-en-hardening se mergearon el 25-08). Cola accionable restante: **0**.

## Números

Tests 418 → **464** · typecheck y build en verde en la rama Y en main tras
el merge · esquema 9 → **10** (aditivo: `ordered_at` + índice).
