# Resumen del día — 2026-09-06

> Contexto de sesión. La sesión cruzó la medianoche: las notas fechadas
> «07-09-2026» en `CLAUDE.md` §11, `ESTADO-PRODUCCION.md`,
> `docs/validacion/` y `PLATFORM-AISLAMIENTO.md` (rama `feat/platform-companies`)
> son de esta misma jornada. Rama canónica al cierre: `release/casamable-v4.3`
> @ `8a864be`. Subido a `origin` el 07-09-2026.

## Contexto de partida

El informe de "48-72h" recibido al inicio del día mostró 9-12 worktrees vivos y sin consolidar, con `package.json` y esquema de base de datos tocados en paralelo por varias ramas — el mismo riesgo de merge-debt que llevábamos días señalando, ya materializado a mayor escala.

## 1. Consolidación de ramas de Casamable — cerrada

Rama canónica establecida: **`release/casamable-v4.3`**, ahora en **`8a864be`** (typecheck, `next build` y **717 tests OK / 0 fallos**).

Se mergearon en orden, con verificación de build+test entre cada paso:
- `fix/whatsapp-confirmacion-bugs` (3 commits)
- `feat/beeping-mark-to-send`
- `hunter-discovery` (superset de `hunter-predictivo`, que se descartó como duplicado)
- **La reescritura de la tarde del 05-09** de Óliver (8 commits en `origin/release/casamable-v4.2`) — no eran duplicados del hotfix de la mañana: traían funcionalidad real adicional no incorporada hasta ahora:
  - Proxy que clasifica staff/propietario correctamente (antes un agente no podía responder a clientes, 403 en `/api/mode` y `/api/messages`).
  - Evaluación de direcciones mucho más completa (`address-assessment.ts`: localizadores tipo s/n, km, nave, bloque) frente a la heurística mínima de 29 líneas que había.
  - Plantilla de confirmación aprobada aplicada de forma más limpia.
  - Detección de intención de cancelación en texto libre, soporte de media entrante (audio/imagen), y cancelación después de confirmar un pedido — nada de esto existía en la canónica.
  - Documentación operativa nueva: `CONVERSACION-REGLAS.md`, runbooks de workspace.

Worktrees obsoletos eliminados (ramas conservadas): `whatsapp-confirmacion-bugs`, `beeping-mark-to-send`, `hunter-predictivo`, `hunter-discovery`, `hunter-landing-studio`, `workspace-atencion-cliente`.

Quedan vivos y fuera de la canónica, por decisión explícita: `hunter-end-to-end-v1` (motor "PI Engine" paralelo, archivado sin mergear — evita duplicar el Hunter que ya funciona) y `platform-companies` (producto comercial futuro, aislado adrede).

`feat/landing-ultima-milla` de Óliver se rebasó sin conflictos sobre la canónica (`ecd2db4`).

## 2. Corrección de constantes económicas del Hunter — hecha, con un ajuste pendiente

Rama `fix/hunter-constantes-economicas @ 27c4987` (710 tests OK):
- Picking: 1,70€ → **1,40€** (contrato Beeping real).
- Envío: dos constantes sueltas (4,08€/6,50€, sin contrato citado) → tabla real de tramos casi planos (3,80€ a 4,00€ entre 1-4kg).
- `BASE_DELIVERY_RATE` renombrado a `DEFAULT_ASSUMED_DELIVERY_RATE`, resolviendo una contradicción con `FINANCE-MODEL.md` que afirmaba que ese valor no estaba hardcodeado en ningún sitio.
- **Pendiente antes de mergear:** el picking no estaba incluido en absoluto en el cálculo de margen del scoring (solo en la estimación predictiva) — hay que añadirlo también ahí. Y la penalización de 10 puntos por peso >1kg ya no tiene base de coste real dado que la tarifa es casi plana — hay que reducirla o eliminarla.

## 3. Aislamiento de `platform-companies` — cerrado

Hallazgo crítico evitado: antes de este trabajo, un `docker compose up -d` accidental desde ese checkout en el NAS habría abierto la base de datos real de Casamable con el código de `platform-companies` y migrado encima sin ningún aviso — ningún producto comprobaba `user_version`.

Ahora, por construcción: fichero SQLite distinto, namespace de código renombrado (`src/lib/hunter/` → `src/lib/winner-radar/`), esquema en bloque disjunto (1000-1999 vs 1-999), variables de entorno con prefijo `PLATFORM_*`, y una guarda de arranque que rechaza abrir una base ajena. Verificado con una base sembrada con `user_version=21`: rechazo correcto, 0 tablas creadas.

**Riesgo abierto y aceptado:** 33 tablas y 54 variables heredadas del agente de Casamable (orders, conversations, SHOPIFY_*, RETELL_*, etc.) siguen con el mismo nombre en ambos productos — renombrarlas sería reescribir el agente entero. Mitigado por las capas de fichero/versión/guarda, no eliminado del todo.

## 4. Dos bloqueadores críticos para el despliegue — sin cerrar, prioridad máxima al despertar

> **Superado el 07-09-2026:** el commit en producción se confirmó (`22f8013`,
> `/api/health/live` devuelve `build`) y la migración se hizo 17→30 en el
> NAS con recuentos intactos. Ver `docs/deploy/DEPLOY-REPORT-v4.3-2026-09-07.md`.
> Lo que sigue es histórico.

1. **No sabemos con certeza qué commit corre hoy en el NAS en producción.** La documentación apunta a `67f05c7`/esquema 15 del 02-09, pero el runbook del 05-09 preparaba `fdad99e`. Antes de cualquier otra tarea de desarrollo: sacar `PRODUCTION_COMMIT` real del contenedor en marcha.
2. **La migración de esquema 15→21 solo se ha probado contra un fixture sintético, nunca contra una copia real de la base de producción.** No autorizado ningún despliegue de v4.3 hasta hacerlo contra una copia real (nunca contra la base viva).
3. Falta además una guarda simétrica en la canónica (que Casamable rechace abrir una base con `user_version` superior al suyo) — pendiente, una línea, baja prioridad frente a los dos puntos anteriores.

## 5. Tareas en cola, en orden de ejecución

1. Confirmar `PRODUCTION_COMMIT` real del NAS.
2. Probar migración 15→21 contra copia real de la base de producción.
3. Cerrar los dos ajustes pendientes de `fix/hunter-constantes-economicas` (picking en el scoring, penalización de peso) y mergear.
4. Validación de direcciones (capa determinista de CP + capa IA con OpenAI, `ALERTA_DIRECCION`, bloqueo de `mark-to-send`) — spec ya entregada, aún no iniciada.
5. Auto-despacho tras cooldown de 8h + motor de IA de intención (cancelación vs. duda simple) — spec ya entregada, en cola detrás de la de direcciones (comparten ficheros).
6. Guarda simétrica en la canónica contra `user_version` superior.

## Pendientes de negocio que solo puede resolver Pedro

- Renovar `META_AD_LIBRARY_ACCESS_TOKEN` como token largo de Usuario del Sistema (el usado en el Explorer era de corta duración).
- Responder las 8 preguntas pendientes de Beeping (`payment_method_id`, identificador de producto, webhook secret...).
- Confirmar si Óliver ya tiene acceso propio al NAS para desplegar cuando Pedro esté fuera de casa.
- Decisión Dropea vs. Beeping: ¿se mantienen ambos conectados a las mismas órdenes reales o se corta uno del flujo automático?

## Archivos generados hoy (entregados por chat, no commiteados a ningún repo salvo indicado)

- `prompt-codex-validacion-direccion-ia.md`
- `prompt-codex-rebase-landing-y-auditoria.md`
- `prompt-codex-fix-constantes-hunter.md`
- `spec-auto-despacho-ia-intencion.md`
- `prompt-codex-aislar-platform-companies.md`
