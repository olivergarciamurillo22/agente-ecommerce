# ── Node ──
node_modules/

# ── Next.js / TypeScript ──
.next/
out/
# Caché de compilación incremental — rompe el build de Nixpacks si se sube
*.tsbuildinfo

# ── Env (NUNCA subir: aquí viven los secretos) ──
.env
.env.local
.env*.local
# Config de producción del NAS (copia local para pegar; en el NAS se llama .env)
.env.nas
ENV-NAS-PENDIENTE.local.txt

# ── DB y runtime (SQLite + sesión de WhatsApp) ──
data/
auth/
.restart

# ── Backups (nunca a git) ──
backups/
nas-data/

# ── Logs y salidas locales ──
*.log
npm-debug.log*
# Resultados del redteam (contienen respuestas del modelo con precios reales)
redteam-resultados.json
*.tmp.ts

# ── OS / IDE ──
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*.swo

# ── Claude Code local ──
.claude/*.lock
.claude/settings.local.json

# ── Artefactos del kit original ──
02-kit-agente-whatsapp.zip

# ── QA visual (capturas Playwright, temporales) ──
.playwright-mcp/
