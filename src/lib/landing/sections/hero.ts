import type { ProductCandidate } from "../../hunter/types";
import { esc } from "../escape";
export function hero(c:ProductCandidate):string { const p=c.scoring?.proposedPriceEur; return `<section class="hero" data-bloque="hero"><div class="wrap hero__grid"><div><p class="hero__eyebrow">Una solución sencilla para cada día</p><h1 class="hero__title">${esc(c.name??"Producto por confirmar")}</h1><p class="hero__text">Información verificable, sin promesas inventadas.</p><p class="hero__price">${p?`${p.toFixed(2).replace(".",",")} €`:"Precio por confirmar"}</p><!-- CHECKOUT: configurar variante antes de activar --><button class="btn" type="button">Elegir producto${p?` — ${p.toFixed(2).replace(".",",")} €`:""}</button><small>Pago contra reembolso · Confirma antes del envío</small></div><!-- Plano: producto en uso, luz natural --><div class="media hero__media" aria-hidden="true"></div></div></section>`; }
