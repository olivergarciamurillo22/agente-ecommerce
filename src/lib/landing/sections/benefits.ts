import type { ProductCandidate } from "../../hunter/types"; import { esc } from "../escape";
export function benefits(c:ProductCandidate):string { const items=(c.claims?.length?c.claims:["Compra sencilla","Información clara","Atención antes del envío"]).slice(0,6); return `<section class="benefits" data-bloque="beneficios"><div class="wrap"><h2 class="benefits__title">Lo esencial, sin rodeos</h2><div class="benefits__grid">${items.map(x=>`<article class="benefits__item"><svg aria-hidden="true"><use href="#i-check"></use></svg><h3>${esc(x)}</h3><p>Detalle verificable del producto.</p></article>`).join("")}</div></div></section>`; }
