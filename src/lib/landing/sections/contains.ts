import type {ProductCandidate}from"../../hunter/types";import{esc}from"../escape";
export function contains(c:ProductCandidate):string{const yes=c.specs?.contiene?.split(/[,;]/).map(x=>x.trim()).filter(Boolean)??[],no=c.specs?.no_contiene?.split(/[,;]/).map(x=>x.trim()).filter(Boolean)??[];if(!yes.length&&!no.length)return"";return `<section class="contains" data-bloque="contiene"><div class="wrap contains__grid"><div><h2>Contiene</h2><ul>${yes.map(x=>`<li>✓ ${esc(x)}</li>`).join("")}</ul></div><div><h2>No contiene</h2><ul>${no.map(x=>`<li>× ${esc(x)}</li>`).join("")}</ul></div></div></section>`;}
