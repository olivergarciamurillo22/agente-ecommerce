import type { ProductCandidate } from "../../hunter/types"; import { esc } from "../escape";
export function variants(c:ProductCandidate):string { if(!c.variants?.length)return ""; return `<section class="variants" data-bloque="variantes"><div class="wrap"><h2 class="variants__title">Elige tu variante</h2><div class="variants__grid">${c.variants.map(v=>`<article class="variants__item"><!-- Plano: variante ${esc(v)} --><div class="media"></div><h3>${esc(v)}</h3><!-- CHECKOUT: configurar variante antes de activar --><button class="btn" type="button">Elegir ${esc(v)}</button></article>`).join("")}</div></div></section>`; }
