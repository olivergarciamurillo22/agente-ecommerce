import fs from "node:fs";import path from "node:path";import {convertLanding}from"../src/lib/landing/converter";const i=process.argv.indexOf("--html"),input=process.argv[i+1];if(!input)throw new Error("Uso: npm run landing:sections -- --html <ruta>");const out=path.resolve("outputs",`${path.basename(input,path.extname(input))}-secciones`);console.log(convertLanding(fs.readFileSync(path.resolve(input),"utf8"),out).join("\n"));
