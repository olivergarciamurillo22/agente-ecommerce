import path from "node:path";import {lintLiquidDir}from"../src/lib/landing/lint";const i=process.argv.indexOf("--dir"),dir=process.argv[i+1];if(!dir)throw new Error("Uso: npm run landing:lint -- --dir <secciones>");const issues=lintLiquidDir(path.resolve(dir));if(issues.length){for(const x of issues)console.error(`${x.rule} · ${x.file}: ${x.message}`);process.exit(1)}console.log("Landing válida: todas las comprobaciones han pasado.");
